# Examples

- `context-envelope.json`: complete structured ingestion payload.

For most users, the shorter command is preferable:

```bash
ctx save "Implemented the change and passed the relevant tests."
```
